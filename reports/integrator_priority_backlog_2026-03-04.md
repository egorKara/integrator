# Приоритизированный бэклог integrator (по итогам аудита 2026-03-04)

## Шкалы
- Критичность: `Blocker` / `High` / `Medium` / `Low`.
- Трудозатраты: `S` (до 0.5 дня), `M` (1–2 дня), `L` (3–5 дней), `XL` (более 5 дней).

## Backlog

| ID | Область | Тип | Критичность | Трудозатраты | Действие | Definition of Done (измеримо) |
|---|---|---|---|---|---|---|
| B1 | CI/CD | Инфраструктура | Blocker | M | Стабилизировать CI на GitHub Actions и удерживать зелёный gate `test` на последовательных прогонах. | 1) Последние 3 run workflow `ci` имеют `conclusion=success`; 2) `test-matrix`, `test-windows`, `security`, `build`, `gate` зелёные; 3) артефакты `coverage.xml`, `results.sarif`, `pip-audit-*.json` публикуются на успешном run. |
| B2 | Документация | Рефакторинг | High | S | Актуализировать `docs/DOCS_INDEX.md` как полноценный SSOT-индекс документов. | 1) В индекс добавлены `ARCHITECTURE`, `LLM_SIDECAR`, `INCIDENTS`, `CODE_REVIEW`, `TECHNICAL_CHANGELOG`; 2) автоматическая проверка ссылок проходит без битых ссылок; 3) в PR приложен отчёт link-check. |
| B3 | Документация | Баг | High | S | Устранить противоречия исторических/операционных описаний (`AGENTS.md` scope path, устаревшие формулировки про `app.py`). | 1) Все активные docs ссылаются на актуальный root `C:\integrator`; 2) устаревшие утверждения помечены как historical snapshot; 3) grep-проверка не находит старый scope-путь в активных инструкциях. |
| B4 | Тесты/качество | Фича | High | M | Поднять покрытие модулей ниже 80% до целевого порога и закрепить в quality policy. | 1) `coverage report` показывает >=80% по каждому модулю из целевого списка; 2) добавлены unit-тесты минимум для 5 низкопокрытых модулей; 3) CI coverage-step проходит без исключений. |
| B5 | Observability | Инфраструктура | Medium | M | Формализовать SLI/SLO по доступности RAG/LM и latency CLI-команд на базе preflight/perf baseline. | 1) Создан `docs/SLI_SLO.md` с SLO и порогами; 2) baseline сохраняется в `reports/perf_baseline_*.json` по расписанию/процедуре; 3) добавлен automated check с fail-условием при деградации > заданного порога. |
| B6 | GitHub process | Фича | Medium | S | Добавить машинный отчёт `issues/pr snapshot` в `reports/` с пагинацией API. | 1) Скрипт выгружает open issues и open PR с пагинацией (`per_page=100` + next pages); 2) формирует JSON/MD сводку; 3) аудит может ссылаться на файл-снимок без ручных запросов. |
| B7 | CLI архитектура | Рефакторинг | Medium | L | Продолжить декомпозицию CLI по roadmap (`P2-CLI-1`) без изменения контрактов вывода. | 1) Контракты `--json` и `--json-strict` неизменны (golden tests); 2) `cli.py` уменьшен по объёму и ответственности; 3) smoke + regression tests зелёные. |
| B8 | Perf/roots | Баг | Medium | M | Разобрать причины `status any_failed=true` в perf baseline и нормализовать работу на дефолтных roots. | 1) baseline `status` показывает `any_failed=false`; 2) список причин ошибок roots документирован и покрыт тестами; 3) команда `integrator status` возвращает предсказуемый код по контракту. |
| B9 | Технологический стек | Рефакторинг | Low | M | Синхронизировать стратегию зависимостей (`pyproject` vs `requirements*.txt`) и добавить lock policy. | 1) Описан единый dependency policy в README; 2) добавлен lock-файл выбранного менеджера; 3) CI использует детерминированную установку для минимум одного job. |
| B10 | Product discovery | Фича | Low | L | Закрыть roadmap-пункт `P2-ARCH-1` (исследование event-driven agents) с конкретным RFC. | 1) Подготовлен RFC в `docs/` с impact-анализом на контракты CLI; 2) перечислены этапы внедрения и риски; 3) решение принято и отражено в changelog/roadmap. |

## Рекомендуемая последовательность исполнения
1. `B1` → восстановить доверие к конвейеру поставки.
2. `B2` + `B3` → убрать документационные противоречия и закрепить SSOT.
3. `B4` + `B8` → закрыть фактические quality/perf пробелы.
4. `B5` + `B6` → закрепить наблюдаемость и прозрачность GitHub-статуса.
5. `B7` + `B9` + `B10` → плановый рефакторинг и развитие архитектуры.

## Статус подтверждения (проверка 2026-03-04, текущая сессия)
- Проверено: `B5-B10` закрыты минимальным контуром изменений (код+тесты+доки+CI), статусы синхронизированы в `reports/priority_execution_tracker_2026-03-04.csv` и `reports/priority_execution_report_2026-03-04.md`.
- Проверено: RFC для `B10` принят и внесён в roadmap-документ (`docs/RFC_P2_ARCH_1_EVENT_DRIVEN_AGENTS_2026-03-04.md`) и changelog.
