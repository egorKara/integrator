# Memory Task (T+A=S)

## Тезис (ситуация)
- В рабочем дереве есть файлы с конфликтными маркерами (`<<<<<<<`, `>>>>>>>`), что сигнализирует о незавершённом merge и снижает доверие к состоянию workspace.

## Антитезис (проблема/риски/неизвестное)
- Конфликтные маркеры ломают автоматические сканы/проверки и вводят в заблуждение агентов при анализе.
- Подпроект находится в дереве репозитория, но игнорируется Git; всё равно попадает в локальные обходы и бэкапы.

## Синтез (решение/подход)
- Найти все файлы с конфликтными маркерами в `bhagavad-gita-reprint/`.
- Привести их к валидному состоянию или удалить подпроект из дерева `C:\integrator` (если он не нужен для текущих задач).
- Зафиксировать результат в issue с доказательствами (пути файлов, команды поиска), без секретов.

## Контекст
- Репо/модуль: `integrator` (workspace hygiene)
- Факт: конфликтные маркеры замечены в `bhagavad-gita-reprint/PROJECT_TODO.md`.

## Верификация
- Команды:
  - `python -m integrator run lint --dry-run` (не обяз., для контекста)
  - Поиск конфликтов (пример): `rg \"^<<<<<<<|^>>>>>>>|^=======$\" bhagavad-gita-reprint -n`
- Ожидаемое поведение: конфликтные маркеры отсутствуют, файл(ы) валидны.

## Next atomic step
Найти все `<<<<<<<`/`>>>>>>>` в `bhagavad-gita-reprint/` и перечислить файлы в комментарии.
